{
  "name": "you-bot-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:vercel": "vite build --outDir dist",
    "preview": "vite preview"
  },
  "dependencies": {
    "allotment": "^1.20.2"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^0.20.0"
  },
  "vercel": {
    "buildCommand": "npm run build",
    "outputDirectory": "dist"
  }
}